# 可选：把 PackyAPI Key / Base URL 默认值注入到前端
# 用户在 UI 里还能改，但首次打开会预填
NEXT_PUBLIC_DEFAULT_BASE_URL=https://www.packyapi.com/v1
NEXT_PUBLIC_DEFAULT_MODEL=gpt-image-2
# PACKY_API_KEY=sk-xxx   # 只在服务端读取，不会注入 bundle
